# Persistence attribution v1 (#174)

Generated by `benchmarks/persistence/capture_attribution.py` from `docs/artifacts/174-persistence-attribution-v1.json` — regenerate, never edit.

- producer commit: `008c0d7505a997a1833d6ce266392f6d1ce7f965`
- semantic digest: `231db9079edbdc9dd1ef8950a8cc5a30a798fd0e440a630f7c6db866f05f8144`

## Verdicts

| case | attribution | causal under | masked under |
|---|---|---|---|
| mdlm_dit_plain | **NO DIVERGENCE UNDER THIS CONDITION** | — | cpu/FLASH/empty_like_nan, cpu/MATH/empty_like_nan, cuda/MATH/empty_like_nan |
| mdlm_dit_latent_conditioned | **NO DIVERGENCE UNDER THIS CONDITION** | — | cpu/FLASH/empty_like_nan, cpu/MATH/empty_like_nan, cuda/MATH/empty_like_nan |
| dream_native | **NO DIVERGENCE UNDER THIS CONDITION** | — | cpu/FLASH/empty_like_nan, cpu/MATH/empty_like_nan, cuda/MATH/empty_like_nan |

## Mechanism

- **load_path**: transformers PreTrainedModel.from_pretrained re-materializes every non-persistent buffer with torch.empty_like (uninitialized memory) and relies on _init_weights to give it a value
- **mdlm_dit**: PR 0 finding: MDLMDiTPreTrainedModel._init_weights was a no-op and Rotary is not a *RotaryEmbedding with original_inv_freq, so the upstream re-init branch never ran and model.rotary.inv_freq stayed uninitialized after from_pretrained. FIX: Rotary.reset_parameters (the constructor's own initializer, build_inv_freq) is now called from _init_weights for Rotary modules only
- **dream**: PR 0 finding: DreamPreTrainedModel._init_weights handled nn.Linear/nn.Embedding only and did not delegate to the base class, so DreamRotaryEmbedding.inv_freq was likewise uninitialized after from_pretrained (reclassifying the #184 native_fp finding: not _extend_rope_if_possible, which is a no-op). FIX: constructor and reset_parameters share _install_inv_freq, and _init_weights calls reset_parameters for DreamRotaryEmbedding; Linear/Embedding init unchanged
- **why_intermittent**: adaLN-Zero gates make finite garbage invisible at init; only NaN/Inf bit patterns in the recycled memory surface, and only under an SDPA backend that propagates NaN (CPU math does, CPU flash swallows it) — the process-bistable backend selection recorded in #184

## Collection node set

- run 1: 2684 nodes (`b757df6705bff29b`)
- run 2: 2684 nodes (`b757df6705bff29b`)
- identical: True
- the historical 2088 vs 2089 passed-count difference is not reproducible on this tree/environment (two fresh collections agree); conditional collection sites (collect-time skips, optional imports) remain the candidate explanation and the final gate requires an identical node set across three consecutive full runs

